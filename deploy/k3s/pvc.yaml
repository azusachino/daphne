apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: daphne-data-pvc
  namespace: harus-media
  labels:
    app: daphne
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
